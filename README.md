## Prerequisites

You need Java installed (JRE or JDK).

Check it is installed:

```bash
java -version
```

## Running backgammon

Run the following from the project root.

```bash
java -jar ./executables/BackgammonApp.jar
```
